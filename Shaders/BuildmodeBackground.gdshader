shader_type canvas_item;

uniform vec2 scroll_speed = vec2(0.5, 0.0);

void fragment() {
    vec2 uv = UV;
    uv += scroll_speed * TIME;
    uv = fract(uv);

    vec4 tex = texture(TEXTURE, uv);

    COLOR = tex * COLOR; // preserve modulate (including alpha)
}